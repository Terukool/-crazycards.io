import React from "react";
import { FC } from "react";
import { PlayCardModel } from "../common/playCard.model";

interface PlayCardProps {
    card: PlayCardModel
}

export const PlayCard: FC<PlayCardProps> = ({card}) => {
    
}
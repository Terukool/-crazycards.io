import React, { FC } from "react";
import styles from './CrazyCards.module.scss';

export const CrazyCards: FC = () => {
    return (
        <div className={styles.container}>
            dbb
        </div>
    )
}